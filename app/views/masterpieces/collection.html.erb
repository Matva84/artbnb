<div class="container">
  <div class="row">
    <div class="col">
      <h2>Voici la liste des oeuvres publiées de <%= @user.email %> </h2>
      <ul class="list-group">
        <% @masterpieces.each do |masterpiece|  %>
          <li class="list-group-item mt-3">
            <%= link_to masterpiece.title, masterpiece %>
            <%= link_to 'Supprimer annonce', masterpiece_path(masterpiece.id), class: 'btn btn-info', data: {turbo_method: :delete, turbo_confirm: "Voulez vous supprimer cette oeuvre?"} %>
          </li>
        <% end  %>
      </ul>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col">
      <h2>Voici la liste des réservations sur les oeuvres de <%= @user.email %> </h2>
      <ul class="list-group">
        <% @masterpieces.each do |masterpiece|  %>
          <li class="list-group-item mt-3">
            <%= link_to masterpiece.title, masterpiece %>
            <%= link_to 'Supprimer annonce', masterpiece_path(masterpiece.id), class: 'btn btn-info', data: {turbo_method: :delete, turbo_confirm: "Voulez vous supprimer cette oeuvre?"} %>
          </li>
        <% end  %>
      </ul>
    </div>
  </div>
</div>
